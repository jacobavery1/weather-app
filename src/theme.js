import {
    createTheme
} from "@mui/system";

export const theme = createTheme({
    palette: {
        primary: {
            main: "#2D4FB4"
        },
        secondary: {
            main: "#A5C0CA"
        }
    },
    typography: {
        fontFamily: ['Poppins']
    }
})